{
  "name": "ballinora-match-card",
  "version": "1.0.0",
  "description": "Lovelace card for the Ballinora Match Tracker integration",
  "type": "module",
  "main": "dist/ballinora-match-card.js",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "test": "vitest run"
  },
  "dependencies": {
    "lit": "^3.2.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "happy-dom": "^17.0.2",
    "vitest": "^3.0.5"
  }
}